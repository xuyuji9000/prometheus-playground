apiVersion: v1
kind: Service
metadata: 
  name: prometheus-service
  labels:
    name: prometheus
spec:
  selector: 
    app: prometheus-server 
  ports: 
    - name: prometheus 
      port: 8080
      targetPort: prometheus
  type: NodePort